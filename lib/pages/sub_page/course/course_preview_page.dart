import 'package:flutter/material.dart';

class CoursePreviewPage extends StatelessWidget {
  const CoursePreviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
